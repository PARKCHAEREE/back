# DAILY LOG - 2026-06-06

## 오늘 목표
- `/api/v1/plants/{plantId}/dashboard/timeline`에서 `actualSeries`, `predictionSeries`, `gapSeries`가 빈 배열로 내려오는 원인 분석 및 수정
- Swagger 테스트 과정에서 발생한 인증/인가 및 `users/me` 500 오류 원인 확인
- 시연/검증용 요청 파라미터 정리 및 재현 가능한 상태로 정리

## 완료한 작업

### 1) 대시보드 타임라인 빈 배열 이슈 분석
- 증상: `anomalyMarkers`는 조회되지만 `actualSeries/predictionSeries/gapSeries`가 빈 배열
- 원인: 가상 시간(`virtualNow`)이 CSV 적재 범위(2025-09-26 ~ 2026-02-23)를 벗어난 상태에서 윈도우 조회가 수행됨
- 확인 파일:
  - `src/main/java/com/solarwise/capstonebackend/service/DashboardService.java`
  - `src/main/java/com/solarwise/capstonebackend/service/SimulationService.java`
  - `src/main/java/com/solarwise/capstonebackend/repository/PlantFeatureLogRepository.java`

### 2) 타임라인 조회 로직 보강 (순환/정규화 대응)
- `DashboardService`에 요청 시각을 데이터 범위로 정규화하는 로직 추가:
  - `normalizeToDataRange(...)` 추가
  - `to` 또는 `virtualNow`가 범위를 벗어나면 CSV 시간축 내부로 순환 매핑
  - `windowStart/windowEnd/forecastEnd` 경계 보정
- 데이터 최소/최대 시각 조회를 위해 Repository 메서드 추가:
  - `findTopByPowerPlantIdOrderByMeasuredAtAsc(...)`
- 수정 파일:
  - `src/main/java/com/solarwise/capstonebackend/service/DashboardService.java`
  - `src/main/java/com/solarwise/capstonebackend/repository/PlantFeatureLogRepository.java`

### 3) 관련 테스트 보강
- `DashboardServiceTest`에 타임라인 정규화/윈도우 보정 시나리오 반영
- 수정 파일:
  - `src/test/java/com/solarwise/capstonebackend/DashboardServiceTest.java`

### 4) Swagger 인증 후 `users/me` 500 오류 수정
- 증상: 토큰 인증 후 `GET /api/v1/users/me` 호출 시 500
- 원인: JWT 필터에서 principal을 `Long`으로 저장하는데 컨트롤러에서 `(String)` 캐스팅하여 `ClassCastException` 발생
- 조치:
  - `principal.toString()` 기반으로 `Long` 파싱하도록 수정
  - 동일 패턴이 있던 `PlantFeatureLogController`도 함께 수정
- 수정 파일:
  - `src/main/java/com/solarwise/capstonebackend/controller/UserController.java`
  - `src/main/java/com/solarwise/capstonebackend/controller/PlantFeatureLogController.java`

## 테스트 결과 및 현황
- `./gradlew compileJava` 성공
- `./gradlew test --tests com.solarwise.capstonebackend.DashboardServiceTest` 시도 시, 본 작업과 무관한 기존 테스트 컴파일 오류로 실패
  - 예: `AnomalyControllerIntegrationTest`, `MeasurementServiceTest`, `PlantServiceTest`의 메서드/필드 불일치

## Swagger 검증 파라미터 가이드 (공유 완료)
- 데이터 범위 기준:
  - `to`: `2025-09-26T00:00:00` ~ `2026-02-23T23:00:00`
- 1차 권장:
  - `plantId=1`, `range=DAY`, `futureHours=6`, `to=2026-02-23T12:00:00`
- 인증 관련:
  - Swagger `Authorize`에는 `accessToken`만 입력 (앞에 `Bearer ` 직접 입력 금지)

## 남은 작업 / 권장 작업
1. 전체 테스트 컴파일 오류 정리 후 회귀 테스트 재실행
2. 타임라인 순환 정책을 `SimulationService` 레벨(가상 시간 전진 시점)로 통합할지 검토
3. Swagger에서 `users/me` → `plants` → `dashboard/timeline` 순서로 실제 호출 리허설 1회 진행

---

작성자: 팀 백엔드 (자동 생성)  
작성일: 2026-06-06

# DAILY LOG - 2026-05-31

## 오늘 목표
- 대시보드 시연용 1초 재생 아키텍처를 고정하고 백엔드 제어 API를 안정화한다.
- 실측/예측/괴리/이상 마커를 한 번에 제공하는 타임라인 API를 구현한다.
- 시뮬레이션 트리거 시나리오를 즉시 발생이 아닌 "미래 데이터 조작" 방식으로 맞춘다.
- RDS 데이터 교체 업로드를 실제로 수행한다.

## 완료한 작업
### 1) 시뮬레이션/대시보드 기능 구현
- `SimulationService` 자동 재생(1초=1시간), `start/stop/status` 상태 관리 구현.
- `SimulationController`에 `playback/start`, `playback/stop`, `playback/status` 추가.
- `DashboardService`/`DashboardController`에 `dashboard/timeline` 추가.
  - 응답: `actualSeries`, `predictionSeries`, `gapSeries`, `anomalyMarkers`.
- `AnomalyRepository`에 시간 구간 anomaly 조회 메서드 추가.

### 2) 권한 및 보안 정리
- JWT에 `role` 클레임 포함(`JwtUtil`, `AuthService`).
- `JwtAuthenticationFilter`에서 `ROLE_*` authority 구성.
- 시뮬레이션 조작 API는 `ADMIN`으로 제한, 조회 API(`time`, `playback/status`)는 인증 사용자 허용.

### 3) 시연 정책 반영
- 재생 속도 1초 고정 정책 문서화.
- `trigger-power-anomaly`를 "미래 데이터 조작" 방식으로 전환.
  - 트리거 직후 즉시 반영이 아닌, 현재 정책상 약 5초(=가상시간 5시간) 뒤부터 반영.

### 4) 문서화
- `docs/planning/dashboard-simulation-plan.md`
- `docs/planning/dashboard-simulation-sequential-plan.md`
- `docs/planning/dashboard-timeline-api-contract.md`
- `docs/planning/demo-api-call-sequence.md`

### 5) 테스트
- 신규/수정 테스트 작성 및 실행:
  - `SimulationServiceTest`
  - `DashboardServiceTest`
  - `AuthServiceTest`
- 핵심 변경 구간 테스트 실행 결과: 성공.

### 6) RDS CSV 교체 업로드 실행 결과
- 대상 파일: `data/csv/samples/wooyang_merged_result_full.csv`
- 실제 업로드 과정에서 확인된 이슈:
  - 기본 multipart 제한(1MB) 초과
  - UTF-8 BOM으로 `TIME` 헤더 매핑 실패
- 조치:
  - 서버를 multipart 제한 상향 옵션으로 실행
  - BOM 제거 파일(`wooyang_merged_result_full_nobom.csv`)로 재업로드
- 최종 결과(plantId=1):
  - `totalRows=3640`, `successCount=3640`, `failureCount=0`
  - 기존 데이터 삭제 후 신규 데이터 적재 완료(서비스 로직 기준).

## CSV/GitHub 노출 정책 확인
- `.gitignore`에 아래 규칙 유지 확인:
  - `data/csv/measurements/*.csv`
  - `data/csv/samples/*.csv`
  - `!data/csv/README.md`
- 현재 Git 추적 상태 확인 결과:
  - `data/csv/samples/.gitkeep`만 추적됨
  - CSV 원본/가공 파일은 추적되지 않음

## 남은 작업
- 필요 시 `WeatherDataImportService`에 BOM 자동 제거/헤더 유연 매핑 보강.
- multipart 제한을 설정 파일(`application-*.properties`)에 명시해 재시작 옵션 의존성 제거.
- 프론트 최종 연동 리허설(시연 스크립트 기준) 1회 수행.

